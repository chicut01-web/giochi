{
  "name": "piuccia-games",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "sync:engine": "node scripts/sync-engine.mjs",
    "deploy:scopa": "npm run sync:engine && supabase functions deploy scopa",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0"
  }
}
